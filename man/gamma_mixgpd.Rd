% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamma-mixgpd.R
\name{gamma_mixgpd}
\alias{gamma_mixgpd}
\alias{dGammaMixGPD}
\alias{pGammaMixGPD}
\alias{rGammaMixGPD}
\alias{qGammaMixGPD}
\title{Gamma mixture with a GPD tail}
\usage{
dGammaMixGPD(
  x,
  w,
  shape,
  scale,
  rate = numeric(0),
  param1,
  param2,
  u,
  sigma,
  xi,
  log = 0
)

pGammaMixGPD(
  q,
  w,
  shape,
  scale,
  rate = numeric(0),
  param1,
  param2,
  u,
  sigma,
  xi,
  lower.tail = 1,
  log.p = 0
)

rGammaMixGPD(
  n,
  w,
  shape,
  scale,
  rate = numeric(0),
  param1,
  param2,
  u,
  sigma,
  xi
)

qGammaMixGPD(
  p,
  w,
  shape,
  scale,
  u,
  sigma,
  xi,
  rate = NULL,
  param1 = shape,
  param2 = scale,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}.}

\item{shape, scale}{Numeric vectors of length \eqn{K} giving component shapes and scales.}

\item{rate}{Numeric vector of length \eqn{K} giving component rates (optional alternative to \code{scale}).}

\item{param1, param2}{Generic aliases for \code{shape} and \code{scale}.}

\item{u}{Numeric scalar threshold at which the GPD tail is attached.}

\item{sigma}{Numeric scalar GPD scale parameter; must be positive.}

\item{xi}{Numeric scalar GPD shape parameter.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE, RNG supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in bulk quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Spliced density/CDF/RNG functions return numeric scalars. The quantile function returns a numeric vector
  with the same length as \code{p}.
}
\description{
This topic documents the mixture distribution spliced with a generalized Pareto (GPD) tail above
a threshold \code{u}. The bulk mixture governs \eqn{x<u}. For \eqn{x\ge u}, exceedances follow a GPD
and are weighted by \eqn{1 - F_{mix}(u)} so that the overall distribution remains proper.
}
\details{
The bulk distribution is a Gamma mixture. A generalized Pareto tail is attached above \code{u}.
The tail mass is \eqn{1 - F_{mix}(u)}, where \eqn{F_{mix}} is the Gamma mixture CDF.
}
\section{Functions}{
\itemize{
\item \code{dGammaMixGPD()}: Density of Gamma mixture with GPD tail

\item \code{pGammaMixGPD()}: Distribution Function of Gamma mixture with GPD Tail.

\item \code{rGammaMixGPD()}: random generation(n=1) of Gamma mixture with GPD tail.

\item \code{qGammaMixGPD()}: Quantile Function of Gamma mixture with GPD tail.

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.8, 0.2)
shape <- c(2, 8)
scale <- c(1, 0.5)
u <- 2
dSp <- compileNimble(dGammaMixGPD)
dSp(3.0, w = w, shape = shape, scale = scale, u = u, sigma = 1, xi = 0.2)
qGammaMixGPD(0.99, w = w, shape = shape, scale = scale, u = u, sigma = 1, xi = 0.2)
}

}
