% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03-build-and-run.R
\name{run_mcmc_bundle_manual}
\alias{run_mcmc_bundle_manual}
\title{Run MCMC for a prepared bundle}
\usage{
run_mcmc_bundle_manual(bundle, show_progress = TRUE)
}
\arguments{
\item{bundle}{A \code{dpmixgpd_bundle} from \code{build_nimble_bundle()}.}

\item{show_progress}{Logical; passed to nimble.}
}
\value{
A fitted object of class \code{"mixgpd_fit"}.
}
\description{
Run MCMC for a prepared bundle
}
\examples{
library(nimble)
y <- abs(rnorm(40)) + 0.1
bundle <- build_nimble_bundle(
  y = y,
  backend = "sb",
  kernel = "normal",
  GPD = FALSE,
  components = 3,
  mcmc = list(niter = 200, nburnin = 50, thin = 1, nchains = 1, seed = 1)
)
fit <- run_mcmc_bundle_manual(bundle, show_progress = FALSE)
fit
}
