% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamma-mixgpd.R
\name{gamma_mix}
\alias{gamma_mix}
\alias{dGammaMix}
\alias{pGammaMix}
\alias{rGammaMix}
\alias{qGammaMix}
\title{Gamma mixture distribution}
\usage{
dGammaMix(x, w, shape, scale, rate = numeric(0), param1, param2, log = 0)

pGammaMix(
  q,
  w,
  shape,
  scale,
  rate = numeric(0),
  param1,
  param2,
  lower.tail = 1,
  log.p = 0
)

rGammaMix(n, w, shape, scale, rate = numeric(0), param1, param2)

qGammaMix(
  p,
  w,
  shape,
  scale,
  rate = NULL,
  param1 = shape,
  param2 = scale,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}. The functions normalize \code{w} internally when needed.}

\item{shape, scale}{Numeric vectors of length \eqn{K} giving component shapes and scales.}

\item{rate}{Numeric vector of length \eqn{K} giving component rates (optional alternative to \code{scale}).}

\item{param1, param2}{Generic aliases for \code{shape} and \code{scale}.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE, RNG supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Density/CDF/RNG functions return numeric scalars. The quantile function returns a numeric vector
  with the same length as \code{p}.
}
\description{
This topic documents the finite-mixture version of the distribution.
The density, CDF, and RNG are implemented as \code{nimbleFunction}s so they can be used
inside NIMBLE models. The quantile function is an R function computed by numerical inversion
of the mixture CDF.
}
\details{
Each component \eqn{j} is a Gamma distribution with \code{shape[j]} and \code{scale[j]}.
The argument \code{rate} can be provided instead of \code{scale} (interpreted as \code{scale = 1/rate}).
The generic aliases \code{param1} and \code{param2} are accepted for \code{shape} and \code{scale}.
}
\section{Functions}{
\itemize{
\item \code{dGammaMix()}: Density of Gamma mixture.

\item \code{pGammaMix()}: Distribution Function of Gamma mixture.

\item \code{rGammaMix()}: Random Generation of Gamma mixture.

\item \code{qGammaMix()}: Quantile Function of Gamma mixture.

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.8, 0.2)
shape <- c(2, 8)
scale <- c(1, 0.5)
dM <- compileNimble(dGammaMix)
dM(1.2, w = w, shape = shape, scale = scale)
qGammaMix(0.9, w = w, shape = shape, scale = scale)
}

}
