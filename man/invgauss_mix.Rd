% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invgauss-mixgpd.R
\name{invgauss_mix}
\alias{invgauss_mix}
\alias{dInvGaussMix}
\alias{pInvGaussMix}
\alias{rInvGaussMix}
\alias{qInvGaussMix}
\title{Inverse Gaussian mixture distribution}
\usage{
dInvGaussMix(
  x,
  w,
  mean,
  shape,
  mu = numeric(0),
  lambda = numeric(0),
  param1,
  param2,
  log = 0
)

pInvGaussMix(
  q,
  w,
  mean,
  shape,
  mu = numeric(0),
  lambda = numeric(0),
  param1,
  param2,
  lower.tail = 1,
  log.p = 0
)

rInvGaussMix(
  n,
  w,
  mean,
  shape,
  mu = numeric(0),
  lambda = numeric(0),
  param1,
  param2
)

qInvGaussMix(
  p,
  w,
  mean,
  shape,
  mu = NULL,
  lambda = NULL,
  param1 = mean,
  param2 = shape,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}. The functions normalize \code{w}
internally when needed.}

\item{mean, shape}{Numeric vectors of length \eqn{K} giving component means and shapes.}

\item{mu, lambda}{Alternative names for \code{mean} and \code{shape}.}

\item{param1, param2}{Generic aliases for \code{mean} and \code{shape}.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE,
the RNG implementation supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Density/CDF/RNG functions return numeric scalars. \code{qInvGaussMix} returns a numeric vector
  with the same length as \code{p}.
}
\description{
A finite mixture of inverse Gaussian components provides a flexible bulk model for positive data
with right skewness. Each component is parameterized by its mean \code{mean[j]} and shape
\code{shape[j]} (also accepted as \code{mu[j]} and \code{lambda[j]}).
}
\details{
The density, CDF, and RNG are implemented as \code{nimbleFunction}s for use inside NIMBLE models.
The quantile function is an R function computed by numerical inversion of the mixture CDF.
}
\section{Functions}{
\itemize{
\item \code{dInvGaussMix()}: Inverse Gaussian mixture density

\item \code{pInvGaussMix()}: Distribution function for Inverse Gaussian mixture

\item \code{rInvGaussMix()}: Random generation for Inverse Gaussian mixture

\item \code{qInvGaussMix()}: Quantile function for Inverse Gaussian mixture

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.6, 0.4)
mu <- c(1, 3)
lam <- c(2, 5)
dM <- compileNimble(dInvGaussMix)
dM(1.2, w = w, mean = mu, shape = lam)
qInvGaussMix(0.9, w = w, mean = mu, shape = lam)
}

}
