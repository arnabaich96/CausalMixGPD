% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-causal.R
\name{qte}
\alias{qte}
\title{Quantile Treatment Effect (QTE)}
\usage{
qte(
  object,
  probs = c(0.1, 0.5, 0.9),
  level = 0.95,
  renormalize_weights = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted object returned by [fit.TE()].}

\item{probs}{Vector of quantile levels in (0, 1).}

\item{level}{Credible interval level (default 0.95).}

\item{renormalize_weights}{Logical; if `TRUE`, renormalize mixture weights per draw.}

\item{...}{Reserved for future use.}
}
\value{
A matrix with one row per `probs` and columns `mean`, `sd`, `lower`, `upper`.
}
\description{
Computes posterior summaries of quantile treatment effects:
\deqn{\mathrm{QTE}(\tau) = Q_{Y(1)}(\tau) - Q_{Y(0)}(\tau).}
}
\details{
Currently implemented for *unconditional* Gamma DP mixture fits created by
[fit.TE()] with `y ~ 0` and `kernel = "gamma"` (and `tail = "none"`).
}
