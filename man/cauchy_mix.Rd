% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cauchy-mix.R
\name{cauchy_mix}
\alias{cauchy_mix}
\alias{dCauchyMix}
\alias{pCauchyMix}
\alias{rCauchyMix}
\alias{qCauchyMix}
\title{Cauchy mixture distribution}
\usage{
dCauchyMix(x, w, location, scale, loc = numeric(0), param1, param2, log = 0)

pCauchyMix(
  q,
  w,
  location,
  scale,
  loc = numeric(0),
  param1,
  param2,
  lower.tail = 1,
  log.p = 0
)

rCauchyMix(n, w, location, scale, loc = numeric(0), param1, param2)

qCauchyMix(
  p,
  w,
  location,
  scale,
  loc = location,
  param1 = location,
  param2 = scale,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}. The functions normalize \code{w}
internally when needed.}

\item{location}{Numeric vector of length \eqn{K} giving component locations.}

\item{scale}{Numeric vector of length \eqn{K} giving component scales (must be positive for a proper density).}

\item{loc}{Alias for \code{location}.}

\item{param1, param2}{Generic aliases for \code{location} and \code{scale}.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE,
the RNG implementation supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Density/CDF/RNG functions return numeric scalars. \code{qCauchyMix} returns a numeric
  vector with the same length as \code{p}.
}
\description{
A Cauchy mixture is a heavy-tailed, symmetric finite mixture that can be useful as a robust
bulk model when extreme observations are not well described by a single scale. This file
provides a NIMBLE-compatible density and CDF, a one-draw RNG, and an R-level quantile function
computed by numerical inversion.
}
\details{
Each component \eqn{j} is a Cauchy distribution with location \code{location[j]} and
scale \code{scale[j]}. The arguments \code{loc} and \code{param1} are accepted as aliases for
\code{location}, and \code{param2} is accepted as an alias for \code{scale}.
}
\section{Functions}{
\itemize{
\item \code{dCauchyMix()}: Density of Cauchy mixture.

\item \code{pCauchyMix()}: Distribution Function of Cauchy mixture.

\item \code{rCauchyMix()}: Random Generation of Cauchy mixture.

\item \code{qCauchyMix()}: Quantile Function of Cauchy mixture.

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.7, 0.3)
loc <- c(0, 2)
sc  <- c(1, 0.5)

dC <- compileNimble(dCauchyMix)
dC(0.25, w = w, location = loc, scale = sc)

qCauchyMix(0.9, w = w, location = loc, scale = sc)
}

}
