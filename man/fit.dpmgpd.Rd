% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_dpm_wrappers.R
\name{fit.dpmgpd}
\alias{fit.dpmgpd}
\title{Fit a Dirichlet process mixture with a GPD tail}
\usage{
fit.dpmgpd(
  formula,
  data,
  kernel = c("gamma", "lognormal", "normal", "laplace", "inverse_gaussian", "amoroso",
    "pareto"),
  dp_rep = c("stick_breaking", "crp"),
  dp_ctrl = list(K = 5),
  priors = list(),
  tail_priors = list(),
  mcmc = list(),
  alpha = 0.05,
  trans = list(),
  intercept = TRUE
)
}
\arguments{
\item{formula}{A formula specifying the model, e.g. `y ~ x1 + x2`
for regression or `y ~ 0` for an unconditional model.}

\item{data}{A `data.frame` containing the variables referenced
in `formula`.}

\item{kernel}{Character string selecting the bulk kernel for the
mixture components. One of
`"gamma"`, `"lognormal"`, `"normal"`, `"laplace"`,
`"inverse_gaussian"`, `"amoroso"`, or `"pareto"`.}

\item{dp_rep}{Character string selecting the Dirichlet process
representation, either `"stick_breaking"` or `"crp"`.}

\item{dp_ctrl}{List of control arguments for the DP representation,
such as `K` (truncation level) when `dp_rep = "stick_breaking"`.}

\item{priors}{Optional list of prior hyperparameters for the bulk,
tail, and DP parameters.}

\item{mcmc}{List of MCMC control settings, typically including
elements such as `n_iter`, `burn_in`, `thin`, and `chains`.}

\item{alpha}{Nominal level used for summarizing posterior quantities
(e.g., `alpha = 0.05` for 95\% credible intervals).}

\item{trans}{Optional list describing transformations to apply
to covariates, resolved internally.}

\item{intercept}{Logical; if `TRUE` (default) an intercept column
is included when there is at least one covariate, with the same
behavior as in [fit.dpm()].}
}
\value{
An object of class `"mixgpd_fit"` with the same structure
as [fit.dpm()], but with `tail = "gpd"` in the `spec` slot and
additional tail parameters in `mcmc_draws`.
}
\description{
Fit a semiparametric bulk–tail model combining a Dirichlet process
mixture for the central part of the distribution with a generalized
Pareto tail for exceedances above a covariate-dependent threshold.
The interface mirrors [fit.dpm()] but sets `tail = "gpd"`.
}
\examples{
\dontrun{
set.seed(1)
dat <- data.frame(
  y  = rgamma(200, shape = 2, scale = 1),
  x1 = rnorm(200)
)

fit_tail <- fit.dpmgpd(
  y ~ x1,
  data   = dat,
  kernel = "gamma",
  dp_rep = "stick_breaking",
  dp_ctrl = list(K = 5),
  mcmc   = list(n_iter = 1000, burn_in = 500)
)

print(fit_tail)
summary(fit_tail)
}

}
