% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace-mixgpd.R
\name{laplace_mix}
\alias{laplace_mix}
\alias{dLaplaceMix}
\alias{pLaplaceMix}
\alias{rLaplaceMix}
\alias{qLaplaceMix}
\title{Laplace mixture distribution}
\usage{
dLaplaceMix(x, w, location, scale, param1, param2, log = 0)

pLaplaceMix(q, w, location, scale, param1, param2, lower.tail = 1, log.p = 0)

rLaplaceMix(n, w, location, scale, param1, param2)

qLaplaceMix(
  p,
  w,
  location,
  scale,
  param1 = location,
  param2 = scale,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}. The functions normalize \code{w} internally when needed.}

\item{location, scale}{Numeric vectors of length \eqn{K} giving component locations and scales.}

\item{param1, param2}{Generic aliases for \code{location} and \code{scale}.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE, RNG supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Density/CDF/RNG functions return numeric scalars. The quantile function returns a numeric vector
  with the same length as \code{p}.
}
\description{
This topic documents the finite-mixture version of the distribution.
The density, CDF, and RNG are implemented as \code{nimbleFunction}s so they can be used
inside NIMBLE models. The quantile function is an R function computed by numerical inversion
of the mixture CDF.
}
\details{
Each component \eqn{j} is a Laplace (double-exponential) distribution with location \code{location[j]}
and scale \code{scale[j]}. The generic aliases \code{param1} and \code{param2} are accepted for these parameters.
}
\section{Functions}{
\itemize{
\item \code{dLaplaceMix()}: Density of Laplace mixture

\item \code{pLaplaceMix()}: Cumulative distribution function of Laplace mixture

\item \code{rLaplaceMix()}: Random generation from Laplace mixture

\item \code{qLaplaceMix()}: Quantile function of Laplace mixture

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.5, 0.5)
loc <- c(0, 2)
sc <- c(1, 0.5)
dM <- compileNimble(dLaplaceMix)
dM(1, w = w, location = loc, scale = sc)
qLaplaceMix(0.9, w = w, location = loc, scale = sc)
}

}
