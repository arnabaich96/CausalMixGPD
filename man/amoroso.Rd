% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amoroso-mixgpd.R
\name{amoroso}
\alias{amoroso}
\alias{dAmoroso}
\alias{pAmoroso}
\alias{qAmoroso}
\alias{rAmoroso}
\title{Amoroso distribution}
\usage{
dAmoroso(x, loc, scale, shape1, shape2, log = 0)

pAmoroso(q, loc, scale, shape1, shape2, lower.tail = 1, log.p = 0)

qAmoroso(p, loc, scale, shape1, shape2, lower.tail = TRUE, log.p = FALSE)

rAmoroso(n, loc, scale, shape1, shape2)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{loc}{Numeric scalar location parameter.}

\item{scale}{Numeric scalar scale parameter. A negative value flips the support.}

\item{shape1}{Numeric scalar first shape parameter. Values must be positive for a proper density.}

\item{shape2}{Numeric scalar second shape parameter. The sign affects tail behavior and can flip
the monotonicity of the gamma transform used internally.}

\item{log}{Logical; if \code{TRUE}, return the log-density. In NIMBLE functions this is provided
as an integer flag with \code{0/1}.}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE,
the RNG implementation supports \code{n = 1}.}
}
\value{
\code{dAmoroso} returns a numeric scalar density; \code{pAmoroso} returns a numeric scalar CDF;
  \code{qAmoroso} returns a numeric scalar quantile; \code{rAmoroso} returns one random draw.
}
\description{
The Amoroso family is a flexible four-parameter distribution on a half-line that
includes many common right-skewed models as special cases. In this package it is
used as a positive-support kernel that plays nicely with the peaks-over-threshold
splicing used by the MixGPD constructions.
}
\details{
This topic documents the core Amoroso functions. The density, distribution, and
random-generation functions are implemented as \code{nimbleFunction}s so they can
be called from NIMBLE models and compiled. The quantile function is an ordinary
R function and can be used directly in analysis code.

The parameterization follows \code{loc} (location), \code{scale} (scale), and two
shape parameters \code{shape1} and \code{shape2}. The scale is allowed to be negative,
which flips the support around \code{loc}. When \code{scale > 0}, the support is
\eqn{x \ge loc}; when \code{scale < 0}, the support is \eqn{x \le loc}.
}
\section{Functions}{
\itemize{
\item \code{dAmoroso()}: Density Function of Amoroso Distribution

}}
\seealso{
\code{dAmorosoMix}, \code{dAmorosoMixGPD} for mixture and spliced variants.
}
