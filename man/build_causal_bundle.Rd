% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-causal.R
\name{build_causal_bundle}
\alias{build_causal_bundle}
\title{Build a causal bundle (design + two outcome arms)}
\usage{
build_causal_bundle(
  y,
  X,
  T,
  backend = c("sb", "crp"),
  kernel,
  GPD = FALSE,
  components = NULL,
  J = NULL,
  param_specs = NULL,
  mcmc_outcome = list(niter = 2000, nburnin = 500, thin = 1, nchains = 1, seed = 1),
  mcmc_ps = list(niter = 2000, nburnin = 500, thin = 1, nchains = 1, seed = 1),
  epsilon = 0.025,
  alpha_random = TRUE,
  ps_model = c("logit"),
  ps_prior = list(mean = 0, sd = 2),
  include_intercept = TRUE
)
}
\arguments{
\item{y}{Numeric outcome vector.}

\item{X}{Design matrix or data.frame of covariates (N x P).}

\item{T}{Binary treatment indicator (length N, values 0/1).}

\item{backend}{Character; \code{"sb"} or \code{"crp"} for outcome models. If length 2,
the first entry is used for treated (\code{T=1}) and the second for control (\code{T=0}).}

\item{kernel}{Character kernel name for outcome models. If length 2,
the first entry is used for treated (\code{T=1}) and the second for control (\code{T=0}).}

\item{GPD}{Logical; include GPD tail for outcomes if TRUE. If length 2,
the first entry is used for treated (\code{T=1}) and the second for control (\code{T=0}).}

\item{components}{Deprecated alias for \code{J}. Only one of \code{J} or \code{components}
should be supplied.}

\item{J}{Integer >= 2; truncation parameter for outcome mixtures. If length 2,
the first entry is used for treated (\code{T=1}) and the second for control (\code{T=0}).}

\item{param_specs}{Outcome parameter overrides (same structure as \code{build_nimble_bundle()}).
You can pass a single list used for both arms or a list with \code{con} and \code{trt} entries.}

\item{mcmc_outcome}{MCMC settings list for the outcome bundles.}

\item{mcmc_ps}{MCMC settings list for the PS model.}

\item{epsilon}{Numeric in [0,1) used by outcome bundles for posterior truncation summaries.
If length 2, the first entry is used for treated (\code{T=1}) and the second for control (\code{T=0}).}

\item{alpha_random}{Logical; whether outcome concentration \code{alpha} is stochastic.}

\item{ps_model}{PS model family. Currently supports: \code{"logit"}.}

\item{ps_prior}{Normal prior for PS coefficients. List with \code{mean} and \code{sd}.}

\item{include_intercept}{Logical; if TRUE, an intercept column is prepended to \code{X}
in the PS model.}
}
\value{
A list of class \code{"dpmixgpd_causal_bundle"}.
}
\description{
Creates a causal bundle with:
\itemize{
  \item a propensity score (PS) design model (logistic regression of \code{T} on \code{X})
  \item an outcome bundle for the control arm (\code{T = 0})
  \item an outcome bundle for the treated arm (\code{T = 1})
}
}
\details{
The outcome bundles reuse the existing DPM + optional GPD tail machinery. The PS model is a
lightweight NIMBLE logistic regression with normal priors on coefficients.
}
\examples{
\dontrun{
set.seed(1)
N <- 100
X <- cbind(x1 = rnorm(N), x2 = runif(N))
T <- rbinom(N, 1, plogis(0.3 + 0.5 * X[, 1]))
y <- rexp(N) + 0.1

cb <- build_causal_bundle(
  y = y,
  X = X,
  T = T,
  backend = "sb",
  kernel = "gamma",
  GPD = TRUE,
  J = 10
)
}
}
