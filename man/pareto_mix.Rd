% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pareto-mix.R
\name{pareto_mix}
\alias{pareto_mix}
\alias{dParetoMix}
\alias{pParetoMix}
\alias{rParetoMix}
\alias{qParetoMix}
\title{Pareto mixture distribution}
\usage{
dParetoMix(
  x,
  w,
  scale,
  shape,
  xm = numeric(0),
  alpha = numeric(0),
  param1,
  param2,
  log = 0
)

pParetoMix(
  q,
  w,
  scale,
  shape,
  xm = numeric(0),
  alpha = numeric(0),
  param1,
  param2,
  lower.tail = 1,
  log.p = 0
)

rParetoMix(
  n,
  w,
  scale,
  shape,
  xm = numeric(0),
  alpha = numeric(0),
  param1,
  param2
)

qParetoMix(
  p,
  w,
  scale,
  shape,
  xm = NULL,
  alpha = NULL,
  param1 = scale,
  param2 = shape,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}. The functions normalize \code{w}
internally when needed.}

\item{scale, shape}{Numeric vectors of length \eqn{K} giving component scales and shapes.}

\item{xm, alpha}{Alternative names for \code{scale} and \code{shape}, respectively.}

\item{param1, param2}{Generic aliases for \code{scale} and \code{shape}.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE,
the RNG implementation supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Density/CDF/RNG functions return numeric scalars. \code{qParetoMix} returns a numeric vector
  with the same length as \code{p}.
}
\description{
A Pareto mixture is a finite mixture of Pareto(scale, shape) components. This can be used as a
simple heavy-tailed model when multiple tail regimes are plausible. This file intentionally does
not provide a GPD-spliced version, because a Pareto component already has a power-law tail and
splicing would typically be redundant.
}
\details{
The density, CDF, and RNG are implemented as \code{nimbleFunction}s for use inside NIMBLE models.
The quantile function is an R function computed by numerical inversion of the mixture CDF.
}
\section{Functions}{
\itemize{
\item \code{dParetoMix()}: Density Function for Pareto Mixture

\item \code{pParetoMix()}: Distribution Function for Pareto Mixture

\item \code{rParetoMix()}: Random Generation for Pareto Mixture

\item \code{qParetoMix()}: Quantile Function for Pareto Mixture

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.5, 0.5)
sc <- c(1, 2)
sh <- c(2, 4)
dM <- compileNimble(dParetoMix)
dM(3, w = w, scale = sc, shape = sh)
qParetoMix(0.9, w = w, scale = sc, shape = sh)
}

}
