% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lognormal-mixgpd.R
\name{lognormal_mix}
\alias{lognormal_mix}
\alias{dLognormalMix}
\alias{pLognormalMix}
\alias{rLognormalMix}
\alias{qLognormalMix}
\title{Lognormal mixture distribution}
\usage{
dLognormalMix(
  x,
  w,
  meanlog,
  sdlog,
  mean = numeric(0),
  sd = numeric(0),
  param1,
  param2,
  log = 0
)

pLognormalMix(
  q,
  w,
  meanlog,
  sdlog,
  mean = numeric(0),
  sd = numeric(0),
  param1,
  param2,
  lower.tail = 1,
  log.p = 0
)

rLognormalMix(
  n,
  w,
  meanlog,
  sdlog,
  mean = numeric(0),
  sd = numeric(0),
  param1,
  param2
)

qLognormalMix(
  p,
  w,
  meanlog,
  sdlog,
  mean = NULL,
  sd = NULL,
  param1 = meanlog,
  param2 = sdlog,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}. The functions normalize \code{w} internally when needed.}

\item{meanlog, sdlog}{Numeric vectors of length \eqn{K} giving component log-means and log-standard deviations.}

\item{mean, sd}{Numeric vectors of length \eqn{K} giving component means and standard deviations on the original scale.
If supplied, both must be provided and will be converted to log-scale parameters.}

\item{param1, param2}{Generic aliases for \code{meanlog} and \code{sdlog}.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE, RNG supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Density/CDF/RNG functions return numeric scalars. The quantile function returns a numeric vector
  with the same length as \code{p}.
}
\description{
This topic documents the finite-mixture version of the distribution.
The density, CDF, and RNG are implemented as \code{nimbleFunction}s so they can be used
inside NIMBLE models. The quantile function is an R function computed by numerical inversion
of the mixture CDF.
}
\details{
Each component \eqn{j} is a lognormal distribution with log-scale parameters \code{meanlog[j]} and \code{sdlog[j]}.
For convenience, you may instead provide \code{mean} and \code{sd} on the original scale; these are converted
internally to the corresponding \code{meanlog} and \code{sdlog} values.
The generic aliases \code{param1} and \code{param2} are accepted for \code{meanlog} and \code{sdlog}.
}
\section{Functions}{
\itemize{
\item \code{dLognormalMix()}: Density function for Lognormal mixture

\item \code{pLognormalMix()}: Cumulative distribution function for Lognormal mixture

\item \code{rLognormalMix()}: Random generation for Lognormal mixture

\item \code{qLognormalMix()}: Quantile function for Lognormal mixture

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.7, 0.3)
ml <- c(0, 1)
sl <- c(0.5, 0.2)
dM <- compileNimble(dLognormalMix)
dM(1.0, w = w, meanlog = ml, sdlog = sl)
qLognormalMix(0.9, w = w, meanlog = ml, sdlog = sl)
}

}
