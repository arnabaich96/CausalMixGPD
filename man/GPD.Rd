% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00-gpd-utils.R
\name{GPD}
\alias{GPD}
\alias{dGpd}
\alias{pGpd}
\alias{rGpd}
\alias{qGpd}
\title{Generalized Pareto distribution}
\usage{
dGpd(x, u, sigma, xi, log = 0)

pGpd(q, u, sigma, xi, lower.tail = 1, log.p = 0)

rGpd(n, u, sigma, xi)

qGpd(p, u, sigma, xi, lower.tail = TRUE, log.p = FALSE)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{u}{Numeric scalar threshold parameter. Values below \code{u} have zero density under the GPD.}

\item{sigma}{Numeric scalar scale parameter; must be positive.}

\item{xi}{Numeric scalar shape parameter. When \code{xi < 0}, the distribution has a finite upper endpoint.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE,
the RNG implementation supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}
}
\value{
\code{dGpd} returns a numeric scalar density; \code{pGpd} returns a numeric scalar CDF;
  \code{qGpd} returns a numeric vector of quantiles; \code{rGpd} returns one random draw.
}
\description{
The generalized Pareto distribution (GPD) is the standard model for threshold exceedances in
peaks-over-threshold methods. In this package it provides the tail component used to splice
onto a bulk model. The implementation follows the common threshold parameterization
\eqn{X \ge u} with scale \code{sigma} and shape \code{xi}.
}
\details{
The density, distribution, and random-generation functions are implemented as
\code{nimbleFunction}s for use inside NIMBLE models. The quantile function is an R function
and is also used internally by mixture+GPD quantile functions.
}
\section{Functions}{
\itemize{
\item \code{dGpd()}: Density of GPD with threshold u

\item \code{pGpd()}: density of GPD with threshold u

\item \code{rGpd()}: Random number Generator

\item \code{qGpd()}: Quantile function

}}
\examples{
\dontrun{
library(nimble)
d <- compileNimble(dGpd)
d(3, u = 2, sigma = 1, xi = 0.2)
qGpd(c(0.5, 0.9), u = 2, sigma = 1, xi = 0.2)
}

}
