% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lognormal-mixgpd.R
\name{lognormal_mixgpd}
\alias{lognormal_mixgpd}
\alias{dLognormalMixGPD}
\alias{pLognormalMixGPD}
\alias{rLognormalMixGPD}
\alias{qLognormalMixGPD}
\title{Lognormal mixture with a GPD tail}
\usage{
dLognormalMixGPD(
  x,
  w,
  meanlog,
  sdlog,
  mean = numeric(0),
  sd = numeric(0),
  param1,
  param2,
  u,
  sigma,
  xi,
  log = 0
)

pLognormalMixGPD(
  q,
  w,
  meanlog,
  sdlog,
  mean = numeric(0),
  sd = numeric(0),
  param1,
  param2,
  u,
  sigma,
  xi,
  lower.tail = 1,
  log.p = 0
)

rLognormalMixGPD(
  n,
  w,
  meanlog,
  sdlog,
  mean = numeric(0),
  sd = numeric(0),
  param1,
  param2,
  u,
  sigma,
  xi
)

qLognormalMixGPD(
  p,
  w,
  meanlog,
  sdlog,
  u,
  sigma,
  xi,
  mean = NULL,
  sd = NULL,
  param1 = meanlog,
  param2 = sdlog,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}.}

\item{meanlog, sdlog}{Numeric vectors of length \eqn{K} giving component log-means and log-standard deviations.}

\item{mean, sd}{Numeric vectors of length \eqn{K} giving component means and standard deviations on the original scale.}

\item{param1, param2}{Generic aliases for \code{meanlog} and \code{sdlog}.}

\item{u}{Numeric scalar threshold at which the GPD tail is attached.}

\item{sigma}{Numeric scalar GPD scale parameter; must be positive.}

\item{xi}{Numeric scalar GPD shape parameter.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE, RNG supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in bulk quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Spliced density/CDF/RNG functions return numeric scalars. The quantile function returns a numeric vector
  with the same length as \code{p}.
}
\description{
This topic documents the mixture distribution spliced with a generalized Pareto (GPD) tail above
a threshold \code{u}. The bulk mixture governs \eqn{x<u}. For \eqn{x\ge u}, exceedances follow a GPD
and are weighted by \eqn{1 - F_{mix}(u)} so that the overall distribution remains proper.
}
\details{
The bulk distribution is a lognormal mixture. A generalized Pareto tail is attached above \code{u}.
}
\section{Functions}{
\itemize{
\item \code{dLognormalMixGPD()}: Density function for Lognormal mixture with GPD tail.

\item \code{pLognormalMixGPD()}: Cumulative distribution function for Lognormal mixture with GPD tail.

\item \code{rLognormalMixGPD()}: Random generation for Lognormal mixture with GPD tail.

\item \code{qLognormalMixGPD()}: Quantile function for Lognormal mixture with GPD tail.

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.7, 0.3)
ml <- c(0, 1)
sl <- c(0.5, 0.2)
u <- 2
dSp <- compileNimble(dLognormalMixGPD)
dSp(3.0, w = w, meanlog = ml, sdlog = sl, u = u, sigma = 1, xi = 0.2)
qLognormalMixGPD(0.99, w = w, meanlog = ml, sdlog = sl, u = u, sigma = 1, xi = 0.2)
}

}
