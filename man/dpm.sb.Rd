% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dpm-sb.R
\name{dpm.sb}
\alias{dpm.sb}
\title{Fit a stick-breaking Dirichlet process mixture model}
\usage{
dpm.sb(
  formula,
  data,
  kernel,
  num_comp = 3,
  mcmc = list(niter = 2000, nburn = 1000, nthin = 1, nchains = 1),
  alpha = 1
)
}
\arguments{
\item{formula}{Model formula. Use \code{y ~ 1} for an unconditional model,
or include covariates on the right-hand side for a regression model.}

\item{data}{A data frame containing variables referenced in \code{formula}.}

\item{kernel}{Character scalar naming the kernel (for example \code{"gamma"},
\code{"normal"}, or \code{"lognormal"}).}

\item{num_comp}{Positive integer giving the truncation level (number of
mixture components). Defaults to \code{3}.}

\item{mcmc}{List of MCMC controls with entries \code{niter}, \code{nburn},
\code{nthin}, \code{nchains}. See details.}

\item{alpha}{Positive concentration parameter for the stick-breaking prior.}
}
\value{
A fitted object of class \code{"mixgpd_fit_dpm_sb"}.
}
\description{
Fits a Dirichlet process mixture model for a single outcome using a
finite stick-breaking truncation with \code{num_comp} mixture components.
The kernel must be a two-parameter kernel supported by the package's
Param1/Param2 interface.
}
\details{
The fitted object is of class \code{"mixgpd_fit_dpm_sb"} and supports
\code{print()}, \code{summary()}, \code{predict()}, \code{fitted()}, and
\code{plot()}.
}
\examples{
\dontrun{
fit <- dpm.sb(y ~ 1, data = my_data, kernel = "gamma", num_comp = 3,
              mcmc = list(niter = 2000, nburn = 1000))
print(fit)
yhat <- fitted(fit)
}
}
