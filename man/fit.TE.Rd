% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-causal.R
\name{fit.TE}
\alias{fit.TE}
\title{Fit treatment-effects model via two-arm DPM or DPM+GPD}
\usage{
fit.TE(
  formula,
  data,
  A,
  kernel = "gamma",
  dp_rep = "stick_breaking",
  dp_ctrl = list(K = 10),
  mcmc = list(n_iter = 2000, burn_in = 1000, chains = 1),
  alpha = 0.05,
  priors = NULL,
  trans = NULL,
  intercept = NULL,
  tail = NULL,
  tail_ctrl = NULL,
  ...
)
}
\arguments{
\item{formula}{A model formula for the outcome.}

\item{data}{A data.frame.}

\item{A}{Treatment indicator. Either a column name in \code{data} or a 0/1 vector.}

\item{kernel}{Bulk kernel(s). Length 1 uses the same kernel for both arms;
length 2 uses \code{kernel[1]} for treated and \code{kernel[2]} for control.}

\item{dp_rep}{DP representation. Passed to the arm fits.}

\item{dp_ctrl}{DP control list (e.g., \code{list(K=5)}). Passed to arm fits.}

\item{mcmc}{MCMC control list. Passed to arm fits.}

\item{alpha}{Credible interval alpha (e.g., 0.05 gives 95\% intervals).}

\item{priors}{Prior specification(s). Length 1 applies to both arms; length 2
uses arm-specific priors.}

\item{trans}{Transform specification(s). Length 1 applies to both arms; length 2 arm-specific.}

\item{intercept}{Logical. Whether to include an intercept in the design matrix construction.
Length 1 applies to both arms; length 2 arm-specific.}

\item{tail}{Tail indicator(s). \code{NULL} or \code{FALSE} uses bulk-only DPM.
\code{TRUE} uses DPM+GPD. Length 1 applies to both arms; length 2 uses
\code{tail[1]} for treated and \code{tail[2]} for control.}

\item{tail_ctrl}{Optional tail control list(s) used only for arms with \code{tail=TRUE}.
Length 1 applies to both tail arms; length 2 arm-specific.}

\item{...}{Reserved for future extensions. Currently unused.}
}
\value{
An object of class \code{"mixgpd_te_fit"} with components
  \code{fit_trt}, \code{fit_con}, \code{spec_trt}, and \code{spec_con}.
}
\description{
Fits separate outcome models for the treated and control groups, then
returns an object suitable for treatment-effect summaries (ATE/QTE).
The fitting backend is chosen per arm using the \code{tail} argument:
\code{tail = FALSE} uses \code{\link{fit.dpm}}, and \code{tail = TRUE}
uses \code{\link{fit.dpmgpd}}.
}
