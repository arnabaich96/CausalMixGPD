% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04-S3-Methods.R
\name{residuals.mixgpd_fit}
\alias{residuals.mixgpd_fit}
\title{Residual-style diagnostics for a MixGPD fit}
\usage{
\method{residuals}{mixgpd_fit}(object, type = c("pit"), ...)
}
\arguments{
\item{object}{A fitted object of class \code{"mixgpd_fit"}.}

\item{type}{Residual type. Only \code{"pit"} is implemented here.}

\item{...}{Unused.}
}
\value{
Numeric vector of residuals (length nobs).
}
\description{
Currently supports PIT (probability integral transform) residuals only when
a working \code{predict(type="survival")} is available for the training data.
}
\examples{
\dontrun{
y <- abs(stats::rnorm(50)) + 0.1
bundle <- build_nimble_bundle(y = y, backend = "sb", kernel = "normal",
                             GPD = TRUE, components = 6,
                             mcmc = list(niter = 200, nburnin = 50, thin = 1, nchains = 1))
fit <- run_mcmc_bundle_manual(bundle)
pit <- residuals(fit, type = "pit")
}
}
