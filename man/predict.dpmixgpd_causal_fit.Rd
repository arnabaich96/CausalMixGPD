% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-causal.R
\name{predict.dpmixgpd_causal_fit}
\alias{predict.dpmixgpd_causal_fit}
\title{Predict from a causal fit}
\usage{
\method{predict}{dpmixgpd_causal_fit}(
  object,
  x = NULL,
  y = NULL,
  ps = NULL,
  newdata = NULL,
  type = c("mean", "quantile", "density", "prob"),
  p = NULL,
  nsim = NULL,
  interval = c("none", "credible"),
  probs = c(0.025, 0.5, 0.975),
  store_draws = TRUE,
  nsim_mean = 200L,
  ncores = 1L,
  ...
)
}
\arguments{
\item{object}{A \code{"dpmixgpd_causal_fit"} object returned by
\code{run_mcmc_causal()}.}

\item{x}{Optional new data. Alias for \code{newdata}.}

\item{y}{Numeric vector of evaluation points (required for \code{type="density"} or \code{"survival"}).}

\item{ps}{Optional numeric vector of propensity scores aligned with \code{x}
/ \code{newdata}. When provided, the supplied scores are used instead of
recomputing them from the stored PS model (needed only for custom inputs).}

\item{newdata}{Optional new data. If \code{NULL}, uses training design (if stored).}

\item{type}{Prediction type. Supported: \code{"mean"}, \code{"quantile"},
\code{"density"}, \code{"prob"}.}

\item{p}{Numeric vector of probabilities for quantiles (required for \code{type="quantile"}).}

\item{nsim}{Number of posterior predictive samples (for \code{type="sample"}).}

\item{interval}{\code{"none"} or \code{"credible"} for posterior credible bands.}

\item{probs}{Quantiles for credible interval bands.}

\item{store_draws}{Logical; whether to store all posterior draws (for \code{type="sample"}).}

\item{nsim_mean}{Number of posterior predictive samples to use for posterior mean estimation (for \code{type="mean"}).}

\item{ncores}{Number of CPU cores to use for parallel prediction (if supported).}

\item{...}{Unused.}
}
\value{
For \code{"mean"} or \code{"quantile"}, a numeric matrix with columns
  \code{ps}, \code{estimate}, \code{lower}, \code{upper}, representing
  treated-minus-control posterior summaries. When PS is disabled or X is absent,
  \code{ps} is \code{NA} and no PS is used. For \code{"density"} and \code{"prob"},
  a data frame with columns \code{y}, \code{ps}, \code{trt_estimate}, \code{trt_lower},
  \code{trt_upper}, \code{con_estimate}, \code{con_lower}, \code{con_upper}.
}
\description{
Provides a unified interface to the treated and control outcome models while
guaranteeing that the same propensity scores are used for both arms. For new
covariates, the PS model stored in \code{object$ps_fit} is used to estimate
the required scores unless the user supplies their own via \code{ps}. If the
bundle was built with \code{PS=FALSE}, the PS model does not exist and outcome
predictions use only the original covariates \code{X}.
}
\examples{
\dontrun{
cb <- build_causal_bundle(y = y, X = X, T = T, backend = "sb", kernel = "normal")
fit <- run_mcmc_causal(cb)
predict(fit, x = X[1:10, ], type = "quantile", p = c(0.25, 0.5, 0.75))
}
}
