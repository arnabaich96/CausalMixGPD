% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normal-mixgpd.R
\name{normal_mix}
\alias{normal_mix}
\alias{dNormMix}
\alias{pNormMix}
\alias{rNormMix}
\alias{qNormMix}
\title{Normal mixture distribution}
\usage{
dNormMix(x, w, mean, sd, var = numeric(0), param1, param2, log = 0)

pNormMix(
  q,
  w,
  mean,
  sd,
  var = numeric(0),
  param1,
  param2,
  lower.tail = 1,
  log.p = 0
)

rNormMix(n, w, mean, sd, var = numeric(0), param1, param2)

qNormMix(
  p,
  w,
  mean,
  sd,
  var = NULL,
  param1 = mean,
  param2 = sd,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{w}{Numeric vector of mixture weights of length \eqn{K}. The functions normalize \code{w} internally when needed.}

\item{mean, sd}{Numeric vectors of length \eqn{K} giving component means and standard deviations.}

\item{var}{Numeric vector of length \eqn{K} giving component variances (optional alternative to \code{sd}).}

\item{param1, param2}{Generic aliases for \code{mean} and \code{sd}.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE, RNG supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function.}

\item{tol}{Numeric scalar tolerance passed to \code{stats::uniroot} in quantile inversion.}

\item{maxiter}{Integer maximum number of iterations for \code{stats::uniroot}.}
}
\value{
Density/CDF/RNG functions return numeric scalars. The quantile function returns a numeric vector
  with the same length as \code{p}.
}
\description{
This topic documents the finite-mixture version of the distribution.
The density, CDF, and RNG are implemented as \code{nimbleFunction}s so they can be used
inside NIMBLE models. The quantile function is an R function computed by numerical inversion
of the mixture CDF.
}
\details{
Each component \eqn{j} is a Normal distribution with mean \code{mean[j]} and standard deviation \code{sd[j]}.
The argument \code{var} can be provided instead of \code{sd} (interpreted as \code{sd = sqrt(var)}).
The generic aliases \code{param1} and \code{param2} are accepted for \code{mean} and \code{sd}.
}
\section{Functions}{
\itemize{
\item \code{dNormMix()}: Normal mixture density

\item \code{pNormMix()}: Normal mixture Distribution function

\item \code{rNormMix()}: Normal mixture random generation

\item \code{qNormMix()}: Normal mixture quantile function

}}
\examples{
\dontrun{
library(nimble)
w <- c(0.6, 0.4)
mu <- c(0, 3)
s <- c(1, 0.8)
dM <- compileNimble(dNormMix)
dM(0.5, w = w, mean = mu, sd = s)
qNormMix(0.95, w = w, mean = mu, sd = s)
}

}
