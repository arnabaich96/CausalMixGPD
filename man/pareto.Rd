% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pareto-mix.R
\name{pareto}
\alias{pareto}
\alias{dPareto}
\alias{pPareto}
\alias{rPareto}
\title{Pareto distribution}
\usage{
dPareto(x, scale, shape, log = 0)

pPareto(q, scale, shape, lower.tail = 1, log.p = 0)

rPareto(n, scale, shape)
}
\arguments{
\item{x}{Numeric scalar giving the point at which the density is evaluated.}

\item{scale}{Numeric scalar scale (lower bound) parameter; must be positive.}

\item{shape}{Numeric scalar shape parameter; must be positive.}

\item{log}{Logical; if \code{TRUE}, return the log-density (integer flag \code{0/1} in NIMBLE).}

\item{q}{Numeric scalar giving the point at which the distribution function is evaluated.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are returned on the log scale.}

\item{n}{Integer giving the number of draws. For portability inside NIMBLE,
the RNG implementation supports \code{n = 1}.}

\item{p}{Numeric scalar probability in \eqn{(0,1)} for the quantile function (if provided).}
}
\value{
\code{dPareto} returns a numeric scalar density; \code{pPareto} returns a numeric scalar CDF;
  \code{rPareto} returns one random draw.
}
\description{
The Pareto distribution is a classic heavy-tailed model on \eqn{[scale, \infty)}.
In this package it is provided as a custom kernel for NIMBLE, with a simple
\code{scale}/\code{shape} parameterization. The implementation is useful both on its own
and as a building block for finite mixtures.
}
\examples{
\dontrun{
library(nimble)
dP <- compileNimble(dPareto)
dP(3, scale = 1, shape = 2)
}

}
