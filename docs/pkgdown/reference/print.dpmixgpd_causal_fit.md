# Print a causal fit

Print a causal fit

## Usage

``` r
# S3 method for class 'dpmixgpd_causal_fit'
print(x, ...)
```

## Arguments

- x:

  A `"dpmixgpd_causal_fit"` object.

- ...:

  Unused.

## Value

The input object (invisibly).
